#include <iostream>
using namespace std;

int main()
{
    float marks;
    cout << "Enter your Marks: ";
    cin >> marks;

    if (marks >= 80 && marks <= 100)
    {
        cout << "A+";
    }
    else if (marks >= 80 && marks <= 100)
    {
        cout << "A+";
    }
    else if (marks >= 80 && marks <= 100)
    {
        cout << "A+";
    }

    return 0;
}
